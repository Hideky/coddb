/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  basePath: "",
  images: {
    unoptimized: true,
    // remotePatterns: [
    //   {
    //     protocol: 'http',
    //     hostname: 'localhost',
    //   },
    // ],
  },
}

export default nextConfig